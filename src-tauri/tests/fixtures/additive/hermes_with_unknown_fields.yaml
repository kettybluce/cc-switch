model:
  default: gpt-4o
  provider: fixture-hermes
  future_model_flag: keep-me

agent:
  max_turns: 50

customTopLevel: keep-me

custom_providers:
  - name: fixture-hermes
    base_url: https://api.fixture.example/v1
    api_key: hermes-live-key
    api_mode: chat_completions
    rate_limit_delay: 0.5
    key_env: FIXTURE_KEY
    foo_bar: keep-me-around
    models:
      gpt-4o:
        context_length: 128000
        future_model_flag: true
