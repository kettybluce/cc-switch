# CC Switch v3.20.10

本仓库独立构建。从官方 **3.20.3** 已合并提交中 **逐条拣选** Wave 1 + Wave 2 + 可选 UI/预设，叠在本 fork SCHEMA 18 之上。

**不是官方 3.20.3。** 没有整段合并官方 `main`，没有采用官方版本号。只发布 **Windows x64**（绿色版优先，MSI 次要）。不构建 Linux / macOS / ARM64。

**请优先使用绿色版（Portable）。** MSI 若出现 Error 5，见 [windows-msi-error-5-zh.md](../windows-msi-error-5-zh.md)。

**SCHEMA 仍为 18。** 没有 `migrate_v18_to_v19`、没有新表、没有改 `proxy_config` CHECK。Codex NTFS 游标复用已有 `session_log_sync.last_byte_offset`。官方 3.20.2 仍能打开这个库文件。Pi 接管标志仍是 `settings.proxy_takeover_pi`。

请从本仓库 v3.20.9（或官方 3.20.2 + 本 fork main）升到本页 v3.20.10。

## 落地清单（相对 v3.20.9）

### Wave 1（必须）

1. **#7227** 空 `reasoning_content` Thought 洪水 — `streaming.rs`
2. **#7280** Codex Chat 合并 commentary + tool_calls，长任务不再提前 stop — `transform_codex_chat.rs`（Moonshot `$ref` sibling 仍在 `forwarder.rs`）
3. **#7219 + follow-up** Codex NTFS 用量字节游标 — `session_usage_codex.rs`（复用 `last_byte_offset`）
4. **禁用 Artifact 工具** 复选框 → `CLAUDE_CODE_DISABLE_ARTIFACT=1`（表单 + i18n）
5. **#7319** 省略 null 工具 description — `transform.rs`（保留 fork 1214 `developer`→`system`）
6. **DeepSeek V4.1 Flash** 定价 seed/repair + **#7286** 视觉目录（仅数据）

### Wave 2（必须）

7. **#7210** 退出/动态端口不再 WHERE-less UPDATE 把 Claude 重试/超时抄到其他应用 — 保留 `proxy_takeover_pi` / 共享 listen
8. **#7263** 省略 `model_provider` 时走本地代理，不再打 `api.openai.com`
9. **#7177** Images follow-ups（完整端点粘贴、大小写后缀、流式用量）— 绕 fork forwarder 拣选
10. **#7212** 统一供应商同步保留子卡 `meta` / `created_at` / `sort_index`

### Wave 3（可选 UI/预设，已含）

11. **#7287** `max_tokens` 1–15 抬到 16（Claude Desktop 探测）
12. **#7330** 智谱 Responses 模型列表 `models[].slug` 回退 — 未改 Pi Option A
13. **#3369** tokens/s 列 — 会话 0.0s / 缺 latency 仍显示 `—`
14. **#7267** 托盘按绑定 ChatGPT 账号显示 Codex 配额
15. **#7318** grok-4.6 / `xhigh` — 与 1214 路径并存
16. **新卡片预设**：Kimi Codex 原生 Responses、MiniMax M3、千问AI平台、聚合商目录。**不改写** 已有 Pi `models.json` 键。

### 刻意跳过

- **#7331** Linux Claude Desktop 3P
- Claude Fable 周限额
- **#7346** npm dist-tags（与 fork misc 冲突）
- Atlas 赞助商身份移除（目录 glm-5.2 已更新，赞助位保留）
- 官方版本号 / 官方 3.20.3 发版说明 / 整段合并官方 main
- 未合并的官方 PR

## 保留的 fork 能力

Pi UNC、共享 Claude listen 接管、`x-cc-switch-app: pi`、1214 `developer`→`system`、Pi CRUD↔`models.json`、默认供应商、拉取模型 Option A、会话用时 `—`、Portable 说明。

## 自测

云端 Linux / GitHub Actions：`pnpm typecheck` / `format:check` / `test:unit`；`cargo fmt` / `clippy` / 相关 `cargo test`。不写用户 `C:` 数据。

**云端测试 ≠ 真机 Windows 冒烟。**

装上后请自己点：

1. Chat 流式上游带空 `reasoning_content` 时，Claude Code 不应刷空 Thought
2. Codex Chat 长任务带进度播报 + 工具调用不应提前 stop
3. Windows 上正在增长的 Codex rollout 用量应继续累加
4. Claude 表单「禁用 Artifact 工具」写入 `CLAUDE_CODE_DISABLE_ARTIFACT=1`
5. 退出应用后 Codex/Gemini 重试与超时不被 Claude 覆盖；Pi 接管仍共享 Claude listen
6. 无 `model_provider` 的 Codex 卡接管后请求应进本地代理
7. 用量表输出 token 旁可出现 tps；Pi/Claude 会话导入用时仍为 `—`
8. MSI Error 5 → 改用绿色版 zip

任一步 FAIL：把日志贴到 issue。SCHEMA 没升，回退 v3.20.9 / 官方 3.20.2 是安全的。

## 下载

- **Windows x64 绿色版（推荐）**：`CC-Switch-v3.20.10-Windows-Portable.zip`
- **Windows x64 安装包**：`CC-Switch-v3.20.10-Windows.msi`
